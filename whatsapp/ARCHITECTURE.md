# 🏗️ Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     Client Applications                      │
│  (Web Browser, Mobile App, Postman, GraphQL IDE, etc.)     │
└────────────┬───────────────────────────────┬────────────────┘
             │                               │
             │ GraphQL API                   │ REST API
             │ (Your Operations)             │ (Meta's Webhooks)
             │                               │
┌────────────▼───────────────────────────────▼────────────────┐
│                    ASP.NET Core Web Host                     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              HTTP Request Pipeline                    │  │
│  │  [HTTPS] → [Routing] → [Controllers/GraphQL]        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─────────────────────┐      ┌──────────────────────────┐ │
│  │   GraphQL Layer     │      │   REST Controller        │ │
│  │                     │      │                          │ │
│  │  • Query            │      │  WebhookController       │ │
│  │    - listTemplates  │      │  • GET  /webhook         │ │
│  │    - getTokenInfo   │      │  • POST /webhook         │ │
│  │                     │      │                          │ │
│  │  • Mutation         │      │  (Meta sends data here)  │ │
│  │    - sendTemplate   │      └──────────┬───────────────┘ │
│  │    - sendText       │                 │                  │
│  │    - clearCache     │                 │                  │
│  └──────────┬──────────┘                 │                  │
│             │                            │                  │
│             └────────────┬───────────────┘                  │
│                          │                                  │
│  ┌───────────────────────▼─────────────────────────────┐   │
│  │              Service Layer (Business Logic)          │   │
│  │                                                      │   │
│  │  ┌────────────────────────────────────────────────┐ │   │
│  │  │         WhatsAppService                        │ │   │
│  │  │  • SendTemplateMessageAsync()                  │ │   │
│  │  │  • SendTextMessageAsync()                      │ │   │
│  │  │  • ListTemplatesAsync()                        │ │   │
│  │  │  • HandleWebhookAsync()                        │ │   │
│  │  │  • ProcessMessagesAsync()                      │ │   │
│  │  │  • HandleIncomingMessageAsync()                │ │   │
│  │  └─────────────────┬──────────────────────────────┘ │   │
│  │                    │                                 │   │
│  │  ┌─────────────────▼──────────────────────────────┐ │   │
│  │  │         TokenManagerService                     │ │   │
│  │  │  • GetAccessTokenAsync()                       │ │   │
│  │  │  • FetchAccessTokenAsync()                     │ │   │
│  │  │  • GetAppAccessTokenAsync()                    │ │   │
│  │  │  • GetSystemUserTokenAsync()                   │ │   │
│  │  │  • CacheToken()                                │ │   │
│  │  └─────────────────┬──────────────────────────────┘ │   │
│  │                    │                                 │   │
│  └────────────────────┼─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────▼─────────────────────────────────┐   │
│  │         Infrastructure Services                       │   │
│  │                                                       │   │
│  │  • IHttpClientFactory  (HTTP requests)               │   │
│  │  • IMemoryCache        (Token caching)               │   │
│  │  • IConfiguration      (Settings)                    │   │
│  │  • ILogger<T>          (Logging)                     │   │
│  └───────────────────────────────────────────────────────┘   │
│                                                              │
└────────────┬─────────────────────────────┬─────────────────┘
             │                             │
             │ HTTP Requests               │ HTTP Requests
             │ (Outbound)                  │ (Outbound)
             │                             │
┌────────────▼──────────────┐   ┌──────────▼──────────────────┐
│   WhatsApp Graph API      │   │   Facebook Graph API        │
│                           │   │                             │
│  • Send Messages          │   │  • OAuth Token Exchange     │
│  • List Templates         │   │  • System User Tokens       │
│  • Message Status         │   │  • Debug Token              │
└───────────────────────────┘   └─────────────────────────────┘
```

## Data Flow Diagrams

### 1. Sending a Message (GraphQL)

```
┌─────────┐      ┌──────────┐      ┌─────────────┐      ┌──────────┐
│ Client  │──1──>│ GraphQL  │──2──>│ WhatsApp    │──3──>│  Token   │
│         │<─6───│ Resolver │<─5───│  Service    │<─4───│ Manager  │
└─────────┘      └──────────┘      └─────────────┘      └──────────┘
                                           │
                                         7 │
                                           ▼
                                    ┌─────────────┐
                                    │  WhatsApp   │
                                    │  Graph API  │
                                    └─────────────┘

1. Client sends GraphQL mutation
2. Resolver calls service method
3. Service requests access token
4. Token Manager returns cached/fresh token
5. Service sends message to WhatsApp API
6. Response flows back through resolver
7. Message delivered to recipient
```

### 2. Webhook Handling (REST)

```
┌──────────┐      ┌──────────────┐      ┌─────────────┐
│ WhatsApp │──1──>│   Webhook    │──2──>│  WhatsApp   │
│   Meta   │<─5───│  Controller  │<─4───│   Service   │
└──────────┘      └──────────────┘      └──────┬──────┘
                                               │
                                             3 │
                                               ▼
                                        ┌──────────────┐
                                        │   Process    │
                                        │   Logic      │
                                        │ • Log event  │
                                        │ • Auto-reply │
                                        │ • Store data │
                                        └──────────────┘

1. Meta sends webhook event
2. Controller validates and parses
3. Service processes event
4. Processing complete
5. 200 OK response to Meta
```

### 3. Token Acquisition Flow

```
┌─────────────┐
│   Service   │
│   Needs     │──1──>┌─────────────────────┐
│   Token     │      │  Token Manager      │
└─────────────┘      └──────────┬──────────┘
                                │
                              2 │ Check Cache
                                │
                     ┌──────────▼──────────┐
                     │   Cache Hit?        │
                     └──────┬──────┬───────┘
                       YES  │      │  NO
                            │      │
                            │      ▼
                            │  ┌──────────────────┐
                            │  │ Strategy 1:      │
                            │  │ Static Token?    │──YES──┐
                            │  └────┬─────────────┘       │
                            │       │ NO                  │
                            │       ▼                     │
                            │  ┌──────────────────┐       │
                            │  │ Strategy 2:      │       │
                            │  │ Env Token?       │──YES──┤
                            │  └────┬─────────────┘       │
                            │       │ NO                  │
                            │       ▼                     │
                            │  ┌──────────────────┐       │
                            │  │ Strategy 3:      │       │
                            │  │ Generate Dynamic │       │
                            │  │ • App Token      │       │
                            │  │ • System Token   │       │
                            │  └────┬─────────────┘       │
                            │       │                     │
                            │       └─────────────────────┤
                            │                             │
                            └─────────────────────────────┘
                                            │
                                            ▼
                                    ┌──────────────┐
                                    │ Cache Token  │
                                    │ (60 days)    │
                                    └──────┬───────┘
                                           │
                                           ▼
                                    ┌──────────────┐
                                    │Return Token  │
                                    └──────────────┘
```

## Component Responsibilities

```
┌────────────────────────────────────────────────────────────┐
│                         Program.cs                          │
│  • Bootstrap application                                    │
│  • Configure services (DI)                                  │
│  • Setup GraphQL server                                     │
│  • Configure middleware pipeline                            │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                    GraphQL/WhatsAppSchema.cs                │
│  • Define GraphQL schema                                    │
│  • Map queries to service methods                           │
│  • Map mutations to service methods                         │
│  • Handle GraphQL-specific concerns                         │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                Controllers/WebhookController.cs             │
│  • Handle webhook verification (GET)                        │
│  • Receive webhook events (POST)                            │
│  • Validate webhook signatures                              │
│  • Forward to service layer                                 │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                   Services/WhatsAppService.cs               │
│  • Core business logic                                      │
│  • WhatsApp API integration                                 │
│  • Message sending (template & text)                        │
│  • Webhook event processing                                 │
│  • Message status handling                                  │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                Services/TokenManagerService.cs              │
│  • Token lifecycle management                               │
│  • Multi-strategy token acquisition                         │
│  • Automatic caching & refresh                              │
│  • Token debugging utilities                                │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                   Models/WhatsAppModels.cs                  │
│  • Data transfer objects (DTOs)                             │
│  • API request/response models                              │
│  • Type-safe data structures                                │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                   Constants/TimeConstants.cs                │
│  • Application-wide constants                               │
│  • Token expiration times                                   │
│  • Cache durations                                          │
└────────────────────────────────────────────────────────────┘
```

## Request Lifecycle

### GraphQL Request (sendTextMessage)

```
1. Client Request
   POST /graphql
   Content-Type: application/json
   {
     "query": "mutation { sendTextMessage(to: \"...\", text: \"...\") { messages { id } } }"
   }

2. ASP.NET Core Routing
   → GraphQL Middleware

3. HotChocolate Processing
   → Parse query
   → Validate against schema
   → Resolve mutation

4. Mutation Resolver
   → Extract parameters
   → Inject WhatsAppService
   → Call SendTextMessageAsync()

5. WhatsAppService
   → Get configuration
   → Request token from TokenManager
   → Build API request
   → Send to WhatsApp API
   → Parse response

6. TokenManagerService
   → Check cache
   → Return cached or fetch new token

7. WhatsApp API
   → Process message
   → Queue for delivery
   → Return message ID

8. Response Flow
   WhatsApp API → Service → Resolver → GraphQL → Client
   
9. Client Receives
   {
     "data": {
       "sendTextMessage": {
         "messages": [
           { "id": "wamid.xxx" }
         ]
       }
     }
   }
```

### Webhook Request (incoming message)

```
1. WhatsApp Event
   User sends message → Meta processes

2. Meta Webhook Call
   POST /webhook
   Content-Type: application/json
   { "object": "whatsapp_business_account", "entry": [...] }

3. ASP.NET Core Routing
   → WebhookController.HandleWebhook()

4. Controller Processing
   → Validate request
   → Parse payload
   → Call service.HandleWebhookAsync()

5. WhatsAppService.HandleWebhookAsync()
   → Validate payload
   → Process entries
   → Handle messages
   → Handle status updates

6. Message Processing
   → Extract message data
   → Log information
   → Optional: Auto-reply
   → Optional: Store in database

7. Response
   → Return 200 OK to Meta
   → Meta stops retry attempts
```

## Technology Stack

```
┌─────────────────────────────────────────────────────────┐
│                    Presentation Layer                    │
│  • HotChocolate.AspNetCore 14.1.0 (GraphQL)            │
│  • ASP.NET Core MVC (REST Controllers)                  │
│  • Banana Cake Pop (GraphQL IDE)                        │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                      Service Layer                       │
│  • C# 12 / .NET 10.0                                    │
│  • Async/Await patterns                                 │
│  • Dependency Injection                                 │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                   Infrastructure Layer                   │
│  • Microsoft.Extensions.Http (HTTP Client)              │
│  • Microsoft.Extensions.Caching.Memory (Caching)        │
│  • Microsoft.Extensions.Configuration (Config)          │
│  • Microsoft.Extensions.Logging (Logging)               │
└─────────────────────────────────────────────────────────┘
                            │
┌─────────────────────────────────────────────────────────┐
│                      External APIs                       │
│  • WhatsApp Business API (graph.facebook.com)           │
│  • Facebook Graph API (OAuth)                           │
└─────────────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                        Internet                          │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│                   Load Balancer                          │
│            (Azure Application Gateway)                   │
└──────────────┬───────────────┬──────────────────────────┘
               │               │
       ┌───────▼──────┐  ┌────▼───────┐
       │  Instance 1  │  │ Instance 2 │
       │              │  │            │
       │ ASP.NET Core │  │ASP.NET Core│
       │  + GraphQL   │  │ + GraphQL  │
       └──────┬───────┘  └────┬───────┘
              │               │
              └───────┬───────┘
                      │
      ┌───────────────▼────────────────┐
      │        Shared Services          │
      │ • Azure Key Vault (Secrets)    │
      │ • Redis Cache (Distributed)    │
      │ • Application Insights (Logs)  │
      └────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Client Request                        │
└──────────────────────┬──────────────────────────────────┘
                       │
              ┌────────▼─────────┐
              │  HTTPS/TLS 1.3   │ ← Transport Security
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │  API Gateway     │ ← Rate Limiting
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │ Authentication   │ ← Optional: JWT/OAuth
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │ Authorization    │ ← Optional: Role-based
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │ Input Validation │ ← GraphQL Schema
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │ Business Logic   │
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │ WhatsApp API     │ ← OAuth Bearer Token
              └──────────────────┘
```

---

## Summary

This architecture provides:
- ✅ **Separation of Concerns** - Clear layer boundaries
- ✅ **Scalability** - Stateless services, cacheable tokens
- ✅ **Maintainability** - Well-organized, documented code
- ✅ **Testability** - Dependency injection, interfaces
- ✅ **Performance** - Async operations, caching
- ✅ **Security** - HTTPS, token management, validation
- ✅ **Flexibility** - GraphQL for queries, REST for webhooks
